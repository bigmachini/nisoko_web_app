# API base URL — defaults to localhost via Vite proxy
VITE_API_BASE_URL=http://localhost:38000

# Comma-separated list of additional allowed hosts (e.g. ngrok tunnels)
# VITE_ALLOWED_HOSTS=abc123.ngrok-free.app
